// Test that non-exhaustive match on union type fails

fun incomplete(x: Int | String) -> String {
    match x {
        n: Int -> show(n)
        // Missing case for String!
    }
}

print(incomplete(42))
