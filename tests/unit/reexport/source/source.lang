package source (Shape, Circle, Square, area)

type Shape = Circle(Float) | Square(Float)

fun area(shape: Shape) -> Float {
    match shape {
        Circle(r) -> 3.14159 * r * r
        Square(s) -> s * s
    }
}
