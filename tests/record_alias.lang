type Point = { x: Int, y: Int }

fun getX(p: Point) -> Int {
    p.x
}

// Pass literal record where Point is expected
p = { x: 10, y: 20 }
print(getX(p))

// Nested alias
type BoxedPoint = { inner: Point }
bp = { inner: { x: 1, y: 2 } }
print(bp.inner.y)

